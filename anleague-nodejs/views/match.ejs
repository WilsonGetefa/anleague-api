<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= title %></title>
  <link href="https://fonts.googleapis.com/css2?family=Roboto:wght@400;700&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="/css/styles.css">
  <link rel="stylesheet" href="/css/match.css">   <!-- NEW -->
  <link rel="icon" type="image/x-icon" href="/images/caf.png">
  <style>
    .match-details { max-width: 600px; margin: 20px auto; padding: 20px; border: 1px solid #ddd; border-radius: 8px; background: #f9f9f9; font-family: 'Roboto', sans-serif; }
    .match-header { text-align: center; margin-bottom: 20px; }
    .team { font-size: 1.4em; font-weight: 600; margin: 0; }
    .vs { color: #666; margin: 0 8px; }
    .score { font-size: 2.2em; font-weight: bold; color: #1a1a1a; margin: 12px 0; }
    .info { margin: 10px 0; font-size: 1em; }
    .status { font-weight: 600; text-transform: capitalize; }
    .status.completed { color: #28a745; } /* green */
    .status.pending, .status.in_progress { color: #FFA500; } /* orange */
    .commentary { margin: 20px 0; padding: 12px; background: #fff; border-left: 4px solid #d4af37; font-style: italic; color: #444; line-height: 1.5; }
    .goals { margin-top: 24px; }
    .goals h3 { margin-bottom: 8px; color: #333; }
    .goal { padding: 6px 0; border-bottom: 1px dashed #eee; font-size: 0.95em; }
    .goal:last-child { border-bottom: none; }
    .back-link { display: block; text-align: center; margin: 25px 0 10px; color: #0066cc; text-decoration: none; font-weight: 500; }
    .back-link:hover { text-decoration: underline; }
  </style>
</head>
<body>
  <header>
    <h1>Match Details</h1>
  </header>

  <nav>
    <a href="/">Home</a> |
    <a href="/bracket">Bracket</a> |
    <a href="/rankings">Rankings</a> |
    <a href="/teams">Teams</a>
    <% if (user) { %>
      <% if (user.role === 'admin') { %>
        <a href="/admin/dashboard">Admin</a> |
      <% } else { %>
        <a href="/dashboard">Dashboard</a> |
      <% } %>
      <a href="/auth/logout">Logout</a>
    <% } else { %>
      <a href="/login">Login</a> |
      <a href="/signup">Sign Up</a>
    <% } %>
  </nav>

  <main class="container">
    <% if (match) { %>
      <div class="match-details">
        <div class="match-header">
          <p class="team"><%= match.team1_id?.country || 'TBD' %> <span class="vs">vs</span> <%= match.team2_id?.country || 'TBD' %></p>
          <div class="score"><%= match.score.team1 %> – <%= match.score.team2 %></div>

          <p class="info">
            <strong>Stage:</strong> <%= match.stage.charAt(0).toUpperCase() + match.stage.slice(1) %> |
            <strong>Status:</strong>
            <span class="status <%= match.status %>">
              <%= match.status.charAt(0).toUpperCase() + match.status.slice(1) %>
            </span>
          </p>
        </div>

        <% if (match.commentary) { %>
          <div class="commentary">
            <strong>Commentary:</strong><br>
            <%= match.commentary %>
          </div>
        <% } %>

        <% if (match.goal_scorers && match.goal_scorers.length) { %>
          <div class="goals">
            <h3>Goal Scorers</h3>
            <% match.goal_scorers.forEach(g => { %>
              <div class="goal">
                <strong><%= g.player_name %></strong>
                (<%= g.minute %>')
                — <%= g.team === 'team1' ? match.team1_id?.country : match.team2_id?.country %>
              </div>
            <% }); %>
          </div>
        <% } %>

        <a href="/bracket" class="back-link">Back to Bracket</a>
      </div>
    <% } else { %>
      <p>Match not found.</p>
    <% } %>
  </main>
</body>
</html>