<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= title %></title>
  <link href="https://fonts.googleapis.com/css2?family=Roboto:wght@400;700&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="/css/styles.css">
  <link rel="icon" type="image/x-icon" href="images/caf.png">
  <style>
    .btn {
      padding: 10px;
      background-color: #2e6b5b;
      color: white;
      border: none;
      border-radius: 4px;
      cursor: pointer;
      font-size: 1.1em;
    }
    .btn:hover {
      background-color: #3a8c76;;
    }
  </style>
</head>
<body>
  <header>
    <h1><%= title %></h1>
  </header>
  <nav>
    <a href="/">Home</a> |
    <a href="/bracket">Bracket</a> |
    <a href="/rankings">Rankings</a> |
    <a href="/teams">Teams</a> |
    <% if (typeof user !== 'undefined' && user) { %> <!-- Safely check user -->
      <% if (user.role === 'admin') { %>
        <a href="/admin/dashboard">Admin Dashboard</a> |
      <% } else { %>
        <a href="/dashboard">Dashboard</a> |
      <% } %>
      <a href="/auth/logout">Logout</a>
    <% } else { %>
      <a href="/login">Login</a> |
      <a href="/signup">Sign Up</a>
    <% } %>
  </nav>
  <main class="container">
    <p>Welcome to the African Nations League! Explore the tournament bracket, check top goal scorers, or sign up to join the competition.</p>
    <% if (currentTournament && currentTournament.status) { %>
      <p>Current Stage: <%= currentTournament.status %></p>
      <a href="/bracket" class="btn">View Bracket</a>
    <% } else { %>
      <p>No active tournament. <% if (user && user.role === 'admin') { %> <a href="/admin/dashboard" class="btn">Start one as admin</a> <% } else { %> Log in as admin to start one. <% } %></p>
    <% } %>
    <h3>Past Champions</h3>
    <ul>
      <% pastWinners.forEach(winner => { %>
        <li><%= winner.year %> - <%= winner.country === 'N/A' ? 'No winner recorded' : winner.country %></li>
      <% }); %>
    </ul>
    <p>Total Goals Scored: <%= totalGoals %></p>
    <a href="/rankings" class="btn">See Full Rankings</a>
    <button type="submit">Start New Tournament</button>
  </main>
</body>
</html>